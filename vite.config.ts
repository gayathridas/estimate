import { defineConfig } from 'vite';

// Set the base path to your GitHub repo name for GitHub Pages deployment.
// e.g. if your repo is https://github.com/gayathridas/estimate, set base: '/estimate/'
export default defineConfig({
  base: '/estimate/',
});
